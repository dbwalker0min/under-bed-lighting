# A sensor for WiFi RSSI
sensor:
  - platform: wifi_signal
    name: ${friendly_name} WiFi Signal
    update_interval: 60s

# A text sensor for the ESPHome version
text_sensor:
  - platform: version
    name: ${friendly_name} ESPHome Version
    